# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project
<!-- One-line description of what this project is. -->

## Workflow
This repo uses the agentic-workflow plugin. Typical loop:
`/discover` → `/plan-feature` → `/decompose` → build → `/code-structure` → `/review-loop`.
Use `/document-architecture` to produce/update `docs/architecture/`.
Use `/handoff` before clearing a heavy thread.

## Standards
<!-- The engineering standards below are shared across all repos. -->

@.cursor/rules/standards.mdc

<!-- Add project-specific, non-obvious context here: architecture layout, folder rules,
     domain boundaries, anything not derivable from the code itself. -->
